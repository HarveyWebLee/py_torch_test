---
description: Git 提交信息规范（Conventional Commits 中文）
alwaysApply: true
---

# 提交规范

格式：`<type>(<scope>): <subject>`

**type**: feat | fix | docs | style | refactor | perf | test | build | ci | chore | revert

**要求**：

- subject 使用中文、祈使语气、不超过 72 字符、句末不加句号
- 仅在用户明确要求时执行 commit
- 提交前确保 `ruff` 与 `pyright` 通过

**示例**：

```
feat(tensor): 添加 GPU 矩阵乘法性能对比脚本
fix(main): 修复无 CUDA 时的设备检测逻辑
chore(deps): 升级 ruff 到 0.9
```
