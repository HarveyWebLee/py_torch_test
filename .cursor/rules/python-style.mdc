---
description: Python 代码风格与类型规范
globs: "**/*.py"
alwaysApply: false
---

# Python 风格

- 使用类型注解：公开函数必须有参数与返回值类型
- 导入顺序：标准库 → 第三方 → 本地（由 ruff isort 自动排序）
- 行宽 88，双引号，4 空格缩进（ruff format 默认）
- 避免 `any`；复杂结构用 `TypedDict` / `dataclass`

```python
# ✅ 设备选择带 CPU 回退
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

# ✅ 公开函数带类型
def train_step(model: nn.Module, batch: torch.Tensor) -> torch.Tensor:
    ...
```
