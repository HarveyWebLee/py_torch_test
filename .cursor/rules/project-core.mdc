---
description: 项目核心约定与 Agent 行为准则
alwaysApply: true
---

# 项目核心约定

- 使用 **uv** 管理依赖与虚拟环境，Python 版本固定为 **3.10.20**
- 代码放在 `src/` 或根目录 `main.py`；实验脚本优先放 `src/`
- 修改逻辑后运行 `uv run ruff format`、`uv run ruff check`、`uv run pyright`
- 回复与注释默认使用**简体中文**；提交信息遵循 Conventional Commits（中文 subject）
- 最小化改动范围，不引入无关依赖或过度抽象
- 仅在用户明确要求时创建 git commit

## 禁止事项

- 不要修改 `uv.lock` 除非依赖变更
- 不要提交 `.venv/`、模型权重、数据集等大文件
- 不要假设 CUDA 一定可用；设备选择需有 CPU 回退
